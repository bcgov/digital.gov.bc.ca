import styled from 'styled-components';
import { Col, Row } from 'react-flexbox-grid';
export const FlexWithGraphicCol = styled(Col).attrs({
    className: 'flexWithGraphicCol',
  })`
    
  `;
  export const FlexWithGraphicRow = styled(Row).attrs({
    className: 'flexWithGraphicRow',
  })`
    
  `;