import React from 'react';

import { CollapseContentHeading } from '../StyleComponents/headings';
import { HrefLink } from '../StyleComponents/htmlTags';
import { saasGuideUrls } from '../../constants/urls';

const RiskManagementReview = () => {
  return (
    <div>
      <p>
        The Risk Management Branch will determine whether the terms relating to
        risk allocations are adequate for the Province or whether changes need
        to be negotiated with the SaaS vendor. Your Legal Services
        representative will likely lead the discussion with the Risk Management
        Branch.
      </p>
    </div>
  );
};

export default RiskManagementReview;
