{{!-- 
  variables
  id: string
  environment: string
  payload: string (json string)
 --}}
### Deployment Created
Deployment {{ id }} to {{ environment }} created :) 

The deployment has the following payload which you can integrate with your ci/cd service.
```json
{{{ payload }}}
```